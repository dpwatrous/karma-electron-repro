{
  "name": "karma-electron-repro",
  "description": "A simple example showing karma hanging after tests succeed",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/dpwatrous/karma-electron-repro"
  },
  "scripts": {
    "test-iframe": "karma start --log-level debug --browsers CustomElectron",
    "test-no-iframe": "karma start --log-level debug --browsers CustomElectron --no-client.useIframe"
  },
  "devDependencies": {
    "electron": "^9.1.0",
    "jasmine-core": "^3.5.0",
    "karma": "^6.4.1",
    "karma-electron": "^6.3.0",
    "karma-jasmine": "^3.3.1"
  }
}
